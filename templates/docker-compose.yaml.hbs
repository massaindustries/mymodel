# Generated by mymodel-cli — do not edit by hand. Run `mymodel init` to regenerate.
services:
  {{projectName}}:
    image: {{image}}
    container_name: {{projectName}}
    ports:
      - "{{port}}:8000"
    volumes:
      - {{configPath}}:/app/config/config.yaml:ro
      - mymodel-models:/app/models
    env_file:
      - {{envPath}}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

volumes:
  mymodel-models:
